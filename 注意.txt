1、更改适合本项目的错误页面
解决之道：在配置上更改。原错误页面的核心代码复制过来
2、jquery验证表单的ajax传递保存问题
解决之道：tp5里面接受url的ajax和a链接带来参数时候用input('param.')接收
3、在搜索时候加字段排序问题
解决之道：搜索字段与排序字段再次+号合并，相当于array_merge
4、自定义验证器类自定义验证方法（如密码强度）
解决之道：上例子
5、验证器类的跨模块（后台或者前台）的验证
解决之道：可分离，字段少的在控制器内写验证
6、表单多选框提交时候input获取不到值
解决之道：$selected = input('post.selected/a');后面加斜杠a即可
7、关联查询的调用方法
8、模板标签if未起作用
亮点：
1、代码生成
2、搜索加排序
3、xunsearch的搜索维护
4、ajax传值